# Veridex Contracts

Hardhat project for the Veridex clearing contract.

```bash
npm install
npm run build
```

Deploy locally (deploy parameters come from `../veridex.config.ts`):

```bash
npm run deploy:local
```
